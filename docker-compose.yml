version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      # Gabungkan di sini, tidak perlu menulis 'volumes:' lagi
      - sqlite_data:/app/data

volumes:
  sqlite_data: